# 🚨 БЫСТРОЕ ИСПРАВЛЕНИЕ ОШИБКИ main.jsx:1 Failed to load resource: 404

## Проблема
GitHub Pages использует исходный `index.html` из корня репозитория вместо собранного из `dist/`.

## ✅ РЕШЕНИЕ (выберите один вариант):

### Вариант 1: Использовать GitHub Actions (РЕКОМЕНДУЕТСЯ) ⭐

Это самый надежный способ - GitHub Actions автоматически будет публиковать правильные файлы.

**Шаги:**

1. **Включите GitHub Pages с GitHub Actions:**
   - Откройте: https://github.com/lvp0110/ag_training_tests/settings/pages
   - В разделе "Source" выберите: **"GitHub Actions"** (не "Deploy from a branch"!)
   - Сохраните

2. **Закоммитьте workflow файл:**
   ```bash
   git add .github/workflows/deploy.yml
   git commit -m "Add GitHub Actions for automatic deployment"
   git push
   ```

3. **Проверьте, что workflow запустился:**
   - Откройте: https://github.com/lvp0110/ag_training_tests/actions
   - Должен появиться workflow "Deploy to GitHub Pages"
   - Дождитесь его завершения (обычно 1-2 минуты)

4. **Готово!** Сайт будет автоматически обновляться при каждом push.

---

### Вариант 2: Ручная настройка через папку docs

Если GitHub Actions не работает, используйте этот способ:

1. **Убедитесь, что файлы в docs/ правильные:**
   ```bash
   ./scripts/fix-deploy.sh
   ```

2. **Закоммитьте:**
   ```bash
   git add docs/
   git commit -m "Fix: Deploy correct files"
   git push
   ```

3. **Настройте GitHub Pages:**
   - Откройте: https://github.com/lvp0110/ag_training_tests/settings/pages
   - Source: **Deploy from a branch**
   - Branch: **main** (или ваша ветка)
   - Folder: **/docs** ⚠️ ОБЯЗАТЕЛЬНО `/docs`, НЕ `/ (root)`!
   - Сохраните

4. **Подождите 1-2 минуты** и проверьте сайт.

---

## 🔍 Как проверить, что проблема решена:

1. Откройте сайт: https://lvp0110.github.io/ag_training_tests/
2. Откройте консоль браузера (F12)
3. **НЕ должно быть ошибки:** `main.jsx:1 Failed to load resource: 404`
4. В Network tab должны загружаться файлы из `/ag_training_tests/assets/`, а не `/src/`

## ⚠️ ВАЖНО:

- **НЕ используйте** исходный `index.html` из корня репозитория для публикации
- **ИСПОЛЬЗУЙТЕ** только файлы из папки `dist/` (или `docs/` после копирования)
- Если видите ошибку `main.jsx:1`, значит GitHub Pages все еще использует исходный файл

## 🆘 Если ничего не помогает:

1. Очистите кэш браузера: Ctrl+Shift+R (Windows) или Cmd+Shift+R (Mac)
2. Проверьте, что в настройках GitHub Pages выбрана правильная папка
3. Убедитесь, что файлы в `docs/index.html` содержат пути `/ag_training_tests/assets/...`, а не `/src/main.jsx`

