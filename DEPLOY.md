# 🚀 Быстрая инструкция по публикации на GitHub Pages

## ⚠️ ПРОБЛЕМА: Ошибка `GET https://lvp0110.github.io/src/main.jsx net::ERR_ABORTED 404`

Эта ошибка возникает, когда на GitHub Pages используется **исходный** `index.html` (из корня репозитория) вместо **собранного** из папки `dist/`.

**Причина:** GitHub Pages настроен на публикацию из корня репозитория, где находится исходный `index.html` с путем `/src/main.jsx`.

## ✅ РЕШЕНИЕ (автоматическое):

### Вариант 1: Использовать готовый скрипт (рекомендуется)

```bash
# Запустите скрипт, который автоматически все сделает
./scripts/fix-deploy.sh
```

Скрипт:
- ✅ Пересоберет проект
- ✅ Скопирует правильные файлы в `docs/`
- ✅ Проверит, что все пути корректны

Затем выполните:
```bash
git add docs/
git commit -m "Fix: Deploy correct files to GitHub Pages"
git push
```

### Вариант 2: Ручная публикация

#### Шаг 1: Соберите проект
```bash
npm run build
```

#### Шаг 2: Скопируйте файлы из `dist/` в папку `docs/`

```bash
# Удалите старую папку docs (если есть)
rm -rf docs

# Создайте новую папку docs
mkdir -p docs

# Скопируйте ВСЕ файлы из dist в docs
cp -r dist/* docs/

# Проверьте, что index.html в docs содержит правильные пути
cat docs/index.html | grep "src="
# Должны быть пути вида: /ag_training_tests/assets/index-XXXXX.js
# НЕ должно быть: /src/main.jsx
```

#### Шаг 3: Закоммитьте и запушьте

```bash
git add docs/
git commit -m "Fix: Deploy correct files to GitHub Pages"
git push
```

### Шаг 4: ⚠️ КРИТИЧЕСКИ ВАЖНО - Настройте GitHub Pages правильно!

1. Откройте настройки репозитория:
   ```
   https://github.com/lvp0110/ag_training_tests/settings/pages
   ```

2. В разделе "Source" выберите:
   - **Deploy from a branch** (НЕ "Deploy from a branch" с корнем!)
   - **Branch:** `main` (или `master` - та ветка, где находится папка `docs/`)
   - **Folder:** `/docs` ⚠️ **ОБЯЗАТЕЛЬНО выберите `/docs`, а не `/ (root)`!**
   
3. Нажмите **"Save"**

4. Подождите 1-2 минуты, пока GitHub Pages обновит сайт

### Шаг 5: Проверьте результат

После публикации (обычно через 1-2 минуты):
1. Откройте ваш сайт: `https://lvp0110.github.io/ag_training_tests/`
2. Откройте консоль браузера (F12)
3. Не должно быть ошибок `main.jsx:1 Failed to load resource`

## 🔍 Как проверить, что всё правильно:

### Проверка 1: Файлы в репозитории

Откройте файлы на GitHub:
- ✅ **Правильно:** `https://github.com/lvp0110/ag_training_tests/blob/main/docs/index.html`
  - Должен содержать: `/ag_training_tests/assets/index-XXXXX.js`
  - НЕ должен содержать: `/src/main.jsx`
  
- ❌ **Неправильно:** `https://github.com/lvp0110/ag_training_tests/blob/main/index.html`
  - Содержит: `/src/main.jsx` (это исходный файл - его НЕ нужно публиковать!)

### Проверка 2: Настройки GitHub Pages

1. Откройте: `https://github.com/lvp0110/ag_training_tests/settings/pages`
2. Убедитесь, что:
   - ✅ Source: **Deploy from a branch**
   - ✅ Branch: **main** (или ваша основная ветка)
   - ✅ Folder: **/docs** ⚠️ **НЕ `/ (root)`!**

### Проверка 3: Опубликованный сайт

1. Откройте: `https://lvp0110.github.io/ag_training_tests/`
2. Откройте консоль браузера (F12)
3. Не должно быть ошибок `main.jsx:1 Failed to load resource`
4. В Network tab должны загружаться файлы из `/ag_training_tests/assets/`, а не `/src/`

## 🎯 Альтернатива: Автоматическая публикация

Если вы хотите автоматическую публикацию при каждом push:

1. В настройках репозитория включите GitHub Pages:
   - `Settings > Pages > Source: GitHub Actions`
2. Закоммитьте файл `.github/workflows/deploy.yml` (уже создан)
3. При каждом push в `main` проект будет автоматически собираться и публиковаться

---

**Помните:** Всегда публикуйте файлы из папки `dist/`, а не исходные файлы!

