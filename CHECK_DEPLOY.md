# 🔍 Проверка статуса деплоя

## Текущая проблема

Ошибки в консоли:
- `GET https://lvp0110.github.io/src/main.jsx` - используется исходный index.html
- `GET https://lvp0110.github.io/AN_GC_logo.jpg` - неправильный путь (должен быть `/ag_training_tests/AN_GC_logo.jpg`)

## ✅ Шаги для проверки и исправления:

### 1. Проверьте статус GitHub Actions

Откройте: https://github.com/lvp0110/ag_training_tests/actions

- Найдите последний workflow "Deploy to GitHub Pages"
- Проверьте, что он **завершился успешно** (зеленая галочка)
- Если он еще выполняется - подождите 1-2 минуты
- Если он завершился с ошибкой - проверьте логи

### 2. Проверьте настройки GitHub Pages

Откройте: https://github.com/lvp0110/ag_training_tests/settings/pages

Убедитесь, что:
- ✅ Source: **GitHub Actions** (не "Deploy from a branch")
- ✅ Последний деплой был недавно (после вашего push)

### 3. Очистите кэш браузера

- **Windows/Linux:** Ctrl + Shift + R
- **Mac:** Cmd + Shift + R
- Или откройте сайт в режиме инкогнито

### 4. Проверьте опубликованный index.html

Откройте в браузере (не на GitHub, а на сайте):
```
https://lvp0110.github.io/ag_training_tests/index.html
```

Затем откройте исходный код страницы (Ctrl+U или Cmd+Option+U) и проверьте:
- ✅ Должен быть путь: `/ag_training_tests/assets/index-XXXXX.js`
- ❌ НЕ должно быть: `/src/main.jsx`

### 5. Если ничего не помогает - принудительный перезапуск

1. Откройте: https://github.com/lvp0110/ag_training_tests/actions
2. Найдите последний успешный workflow
3. Нажмите "Re-run all jobs" (кнопка справа вверху)

Или создайте пустой коммит для перезапуска:
```bash
git commit --allow-empty -m "Trigger deployment"
git push origin main
```

## 🔧 Альтернативное решение (если GitHub Actions не работает)

Если GitHub Actions не работает, используйте ручной деплой через папку `docs/`:

1. **Соберите проект:**
   ```bash
   npm run build
   ```

2. **Скопируйте файлы:**
   ```bash
   rm -rf docs
   mkdir docs
   cp -r dist/* docs/
   ```

3. **Закоммитьте:**
   ```bash
   git add docs/
   git commit -m "Manual deploy to GitHub Pages"
   git push origin main
   ```

4. **Измените настройки GitHub Pages:**
   - Settings > Pages > Source: **Deploy from a branch**
   - Branch: **main**
   - Folder: **/docs**

