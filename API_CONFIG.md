# 🔧 Настройка API для production

## Проблема

При публикации на GitHub Pages возникают ошибки 404 для endpoints `/answers` и `/check`, так как эти пути работают только в локальной разработке через proxy.

## Решение

### Вариант 1: Использовать переменную окружения (рекомендуется)

1. **Создайте файл `.env.production`** в корне проекта:
   ```
   VITE_API_BASE_URL=http://your-api-server.com
   ```
   
   Замените `http://your-api-server.com` на реальный URL вашего API сервера.

2. **Пересоберите проект:**
   ```bash
   npm run build
   ```

3. **Закоммитьте `.env.production`** (если API URL не секретный) или добавьте в `.gitignore`:
   ```
   .env.production
   ```

### Вариант 2: Изменить конфигурацию напрямую

Отредактируйте файл `src/config/api.js` и укажите полный URL к API:

```javascript
export const API_BASE_URL = 'http://your-api-server.com';
```

### Вариант 3: Использовать CORS proxy (для тестирования)

Если API сервер не поддерживает CORS, можно использовать публичный CORS proxy:

```javascript
export const API_BASE_URL = 'https://cors-anywhere.herokuapp.com/http://your-api-server.com';
```

⚠️ **Внимание:** Публичные CORS proxy не рекомендуется для production!

## Текущая конфигурация

- **Разработка (dev):** Используются относительные пути `/answers` и `/check`, которые работают через proxy в `vite.config.js`
- **Production:** По умолчанию используются те же относительные пути. Если API находится на другом домене, установите `VITE_API_BASE_URL`

## Проверка

После настройки:

1. Пересоберите проект: `npm run build`
2. Проверьте, что в собранном коде используются правильные URL
3. Опубликуйте обновленную версию

## Примеры

### Локальный API сервер
```
VITE_API_BASE_URL=http://localhost:3000
```

### Удаленный API сервер
```
VITE_API_BASE_URL=https://api.example.com
```

### API на том же домене, но другой путь
```
VITE_API_BASE_URL=https://lvp0110.github.io/api
```

